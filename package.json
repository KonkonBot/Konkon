{
  "name": "konkon",
  "private": true,
  "scripts": {
    "bot": "bun run --filter bot",
    "api": "bun run --filter api",
    "db": "bun --filter @konkon/db"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^1.7.3",
    "@types/bun": "latest"
  },
  "module": "index.ts",
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "trustedDependencies": [
    "@biomejs/biome",
    "akore"
  ]
}
